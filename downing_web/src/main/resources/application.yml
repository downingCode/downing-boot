server:
  port: 9000

spring:
  application:
    name: downing-web
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/downing_boot?useUnicode=true&amp;characterEncoding=utf8
    username: root
    password:
    hikari:
      auto-commit: false
  security:
    basic:
      enabled: false